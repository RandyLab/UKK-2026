INSERT INTO buku (judul, penulis, stok) VALUES
('Pemrograman Java', 'Budi Santoso', 5),
('Database MySQL', 'Sari Dewi', 3),
('Struktur Data', 'Agus Wijaya', 2),
('Algoritma Dasar', 'Rina Melati', 4);


INSERT INTO anggota (nama, no_hp) VALUES
('Andi Pratama', '08123456789'),
('Rina Wijaya', '08129876543'),
('Budi Santoso', '081311223344');

SELECT * FROM buku;
SELECT * FROM anggota;
SELECT * FROM transaksi;

-- Lihat transaksi aktif
SELECT t.id_transaksi, a.nama, b.judul, t.tanggal_pinjam, t.status
FROM transaksi t
JOIN anggota a ON t.id_anggota = a.id_anggota
JOIN buku b ON t.id_buku = b.id_buku
WHERE t.status = 'dipinjam';

-- Reset transaksi
UPDATE transaksi SET 
    tanggal_kembali = NULL,
    status = 'dipinjam',
    denda = 0;

-- Reset stok buku (contoh: semua jadi stok 5)
UPDATE buku SET stok = 5;